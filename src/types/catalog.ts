export type CatalogItem = { slug: string; name: string; category: string; image: string; shortDescription: string; material?: string; place?: string; };
export type Collection = { slug: string; name: string; introduction: string; itemSlugs: string[]; };
